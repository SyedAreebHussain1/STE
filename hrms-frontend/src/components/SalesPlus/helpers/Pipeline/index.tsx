import KanbanBoard from "./helpers/KanbanBoard";
import { PageContainer } from "../../../../helpers/PageContainer/PageContainer";

const SalesPlusPipeline = ({ selectCampaign }: any) => {
  return (
    <PageContainer>
      <KanbanBoard />
    </PageContainer>
  );
};

export default SalesPlusPipeline;
